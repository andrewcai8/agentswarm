[project]
name = "longshot"
version = "0.1.0"
description = "Massively parallel autonomous coding agent orchestrator"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=14.3.2",
]
